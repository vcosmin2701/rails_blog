</div>
  <h1 class="text-3xl font-bold underline">Articles</h1> 
  <%= link_to "New Article", new_article_path %>
</div>

<div class="flex justify-center">
  <ul class="bg-white rounded-lg shadow ">
    <% @articles.each do |article| %>
      <li class="px-6 py-4">
        <div class="flex justify-between">
          <span class="font-semibold text-lg">  
            <%= link_to article.title, article %>
          </span>
          <span class="text-gray-500 text-xs"><%= article.created_at%></span>
        </div>
        <p class="text-gray-700">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam in dui mauris.</p>
      </li>
    <% end %>
  </ul>
</div>
